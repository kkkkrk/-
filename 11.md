# 자료구조 11 - Graph (그래프)

## Graph (그래프) 기본 개념
- **Vertex** (정점): 노드 혹은 포인트라고도 부름 — 지도의 위치, 사람, 해야 할 일 등
- **Edge** (간선): 아크(Arc) 혹은 라인(Line) — 두 정점을 연결하는 선
- 그래프 G = (V, E) 형태로 표현 (Vertex의 집합 + Edge의 집합)
- **Undirected Edge**: 방향 없음 (양방향)
- **Directed Edge**: 방향 있음 (단방향)
- **Weighted Edge**: 간선에 가중치(값)가 있는 경우
- **Mixed Graph**: Directed + Undirected 혼합 (본 강의에서는 다루지 않음)

#### ✅ 응용 분야
데이터 네트워크(컴퓨터/라우터/스위치 연결), 소셜 네트워크, 지식 그래프(Knowledge Graph), 항공 네트워크(공항 간 연결) 등

---

## 그래프 용어 정리
- **Degree**: 한 정점에 연결된 엣지의 수 (Undirected)
- **In-degree**: 들어오는 엣지 수 (Directed)
- **Out-degree**: 나가는 엣지 수 (Directed)
- **Adjacent (인접)**: 두 정점이 엣지로 연결된 상태
- **Path (경로)**: S→A→B→C→D처럼 이어지는 경로, 거리 = 엣지 수
- **Cycle (사이클)**: 시작 정점으로 다시 돌아오는 경로
- **Acyclic Graph**: 사이클이 없는 그래프
- **Complete Graph**: 모든 정점 쌍 사이에 엣지가 존재
  - Directed: n×(n-1)개의 엣지
  - Undirected: n×(n-1)/2개의 엣지
- **Connected Graph**: 모든 정점이 서로 연결된 그래프

---

## 그래프 표현 방식

### ✅ Adjacency Matrix (인접 행렬)
- 2차원 배열로 표현
- 공간 복잡도: **O(V²)**
- 정점 수가 많고 간선이 적은 희소 그래프(Sparse Graph)에서 비효율적

### ✅ Adjacency List (인접 리스트)
- Linked List로 표현
- 각 정점마다 연결된 정점들의 리스트를 유지
- 희소 그래프에 공간 효율적

```cpp
struct Node {
    int vertex;
    Node* next;
};
// 정점 배열은 1차원 배열, 연결 엣지들은 Linked List로 관리
Node* adjacent[MAX_SIZE];
```

---

## 그래프 탐색

### ✅ DFS (Depth-First Search, 깊이 우선 탐색)
- 한 방향으로 끝까지 가본 뒤 돌아오는 방식
- **Stack** 또는 **재귀(Recursion)** 으로 구현
- 예: S→A→D→B→C

```cpp
void Graph::DFS(int start) {
    vector<bool> visited(size, false);
    Stack<int> stack;
    stack.Push(start);

    while (!stack.IsEmpty()) {
        int v = stack.Pop();
        if (!visited[v]) {
            visited[v] = true;
            cout << label[v] << " ";
            // 이웃 정점들을 스택에 push
            for (int neighbor : adjacent[v])
                if (!visited[neighbor])
                    stack.Push(neighbor);
        }
    }
}
```

### ✅ BFS (Breadth-First Search, 너비 우선 탐색)
- 현재 정점에 인접한 모든 정점을 먼저 방문하는 방식
- **Queue** 로 구현
- 예: S→A→B→C→D

```cpp
void Graph::BFS(int start) {
    vector<bool> visited(size, false);
    Queue<int> q;
    q.Insert(start);
    visited[start] = true;

    while (!q.IsEmpty()) {
        int v = q.Delete();
        cout << label[v] << " ";
        // 이웃 정점들을 큐에 insert
        for (int neighbor : adjacent[v])
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                q.Insert(neighbor);
            }
    }
}
```

> 💡 DFS와 BFS는 코드 구조가 거의 동일 — Stack 쓰면 DFS, Queue 쓰면 BFS

---

## 그래프 응용 알고리즘

### ✅ Find Path (경로 탐색)
- DFS로 탐색하면서 방문 경로를 스택에 저장
- 목적지 도달 후 스택을 Pop하면 역방향으로 경로가 나옴 → 다시 스택에 넣어 순방향 출력
- 경로가 없으면 탐색 실패 메시지 출력

### ✅ Connected Check (연결성 확인)
- DFS 또는 BFS로 시작 정점에서 탐색 후 방문되지 않은 정점이 있으면 비연결 그래프

### ✅ Graph Coloring (그래프 컬러링) — Greedy
- 인접한 정점끼리 같은 색이 되지 않도록 색을 배정
- **Greedy 알고리즘**: 매 정점마다 현재 사용 가능한 첫 번째 색을 바로 배정

```cpp
void Graph::GreedyColoring() {
    vector<int> color(size, -1);
    color[0] = 0;

    for (int v = 1; v < size; v++) {
        vector<bool> available(size, true);
        // 인접 정점의 색 제거
        for (int neighbor : adjacent[v])
            if (color[neighbor] != -1)
                available[color[neighbor]] = false;
        // 사용 가능한 첫 번째 색 선택
        for (int c = 0; c < size; c++) {
            if (available[c]) { color[v] = c; break; }
        }
    }
}
```

### ✅ Union-Find (Disjoint Set, 합집합-찾기)
- 정점들을 그룹(집합)으로 관리
- **find(x)**: x가 속한 집합의 루트(대표)를 반환 (재귀로 부모를 타고 올라감)
- **union(x, y)**: y의 대표의 부모를 x의 대표로 설정 → 두 집합 합치기
- 초기: 각 정점의 parent = 자기 자신

```cpp
int parent[MAX_SIZE];

int find(int x) {
    if (parent[x] == x) return x;
    return find(parent[x]);  // 재귀로 루트 탐색
}

void unionSet(int x, int y) {
    int rootX = find(x);
    int rootY = find(y);
    parent[rootY] = rootX;  // y의 루트를 x의 루트 아래로
}
```

---

## ✅ 시간복잡도 요약

| 연산 | Adjacency Matrix | Adjacency List |
|------|-----------------|----------------|
| 공간 | O(V²) | O(V + E) |
| 간선 확인 | O(1) | O(degree) |
| DFS / BFS | O(V²) | O(V + E) |
| 인접 정점 순회 | O(V) | O(degree) |

---

## ✅ 핵심 정리
- Undirected Graph 중심으로 학습 (다음 강의: Directed Graph)
- DFS → 스택/재귀, BFS → 큐
- 희소 그래프 → Adjacency List가 효율적
- Graph Coloring → Greedy 알고리즘 활용
- Union-Find → Spanning Tree, 사이클 탐지 등에 활용