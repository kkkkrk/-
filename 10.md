## m-ary Tree 표현 방법

### 고정 m일 때
- 자식 수가 최대 m개 → 노드마다 m개의 링크 포인터 사용

### m이 가변일 때 (Left-Child Right-Sibling)
- 노드마다 포인터를 **2개**만 사용
  - **Left child**: 첫 번째 자식
  - **Right sibling**: 다음 형제 노드
- 자식 수를 제한 없이 늘릴 수 있음

## Linked Binary Tree

### Singly vs Doubly Linked Tree
- **Singly Linked Tree**: 루트에서 아래 방향(위→아래)으로만 이동 가능
- **Doubly Linked Tree**: parent 포인터를 추가하여 아래→위 이동도 가능
- 이 과목에서는 주로 **Singly Linked Tree** 사용

### 노드 구조
```cpp
struct Node {
    Data data;
    Node* left;   // 왼쪽 자식
    Node* right;  // 오른쪽 자식
};
```

### ✅ 기본 특성 (n개 노드 기준)
| 항목 | 값 |
|---|---|
| 총 포인터 수 | 2n |
| 실제 링크 수 (엣지) | n - 1 |
| **Null 포인터 수** | **n + 1** |
| 높이 (Skewed Tree) | n |
| 높이 (Complete Binary Tree) | ⌊log₂n⌋ + 1 |

> 배열 기반은 비어있는 공간이 생겨 메모리 낭비 가능 → 링크트 기반은 Complete Binary Tree가 아니어도 효율적


## Tree Traversal (순회)

세 가지 순회 방식 모두 **재귀(DFS)** 로 구현

### Preorder (전위 순회 / MLR)
- 노드 출력 → 왼쪽 서브트리 → 오른쪽 서브트리
- 출력 예: `1 2 4 5 3 6 7`
```cpp
void PrintPreorder(Node* n) {
    if (n == nullptr) return;
    cout << n->data;      // Visit
    PrintPreorder(n->left);
    PrintPreorder(n->right);
}
```

### Inorder (중위 순회 / LMR)
- 왼쪽 서브트리 → 노드 출력 → 오른쪽 서브트리
- 수식 트리에서 **infix** 형태로 출력
```cpp
void PrintInorder(Node* n) {
    if (n == nullptr) return;
    PrintInorder(n->left);
    cout << n->data;      // Visit
    PrintInorder(n->right);
}
```

### Postorder (후위 순회 / LRM)
- 왼쪽 서브트리 → 오른쪽 서브트리 → 노드 출력
- 수식 트리에서 **evaluation(계산)** 에 사용 (postfix)
```cpp
void PrintPostorder(Node* n) {
    if (n == nullptr) return;
    PrintPostorder(n->left);
    PrintPostorder(n->right);
    cout << n->data;      // Visit
}
```

### Level-order (레벨 순회 / BFS)
- **Queue** 를 사용하여 레벨별로 출력
```cpp
void PrintLevelorder(Node* root) {
    Queue q;
    q.Insert(root);
    while (!q.IsEmpty()) {
        Node* n = q.Delete();
        if (n == nullptr) continue;
        cout << n->data;
        q.Insert(n->left);
        q.Insert(n->right);
    }
}
```

### ✅ 시간복잡도
| 연산 | 복잡도 |
|---|---|
| 모든 순회 | O(n) |
| 노드 수 계산 | O(n) |
| 높이 계산 | O(n) |
| 특정 노드 탐색 | O(n) |



## Binary Tree 클래스 구현

```cpp
class BinaryTree {
    Node* root;
public:
    void Insert(Data d, int pos);   // 특정 위치에 삽입
    int  NodeCount(Node* n);        // 노드 수: 1 + left + right
    int  LeafCount(Node* n);        // 리프 노드 수
    int  Height(Node* n);           // 높이
    void PrintInorder(Node* n);
    void PrintPreorder(Node* n);
    void PrintPostorder(Node* n);
    void PrintLevelorder();         // BFS, Queue 사용
};

// 리프 노드: left == nullptr && right == nullptr
bool IsLeaf(Node* n) {
    return n->left == nullptr && n->right == nullptr;
}
```

## Linked Tree로 Linked List 구현

- 왼쪽 자식을 무조건 **null** 로 설정 → 오른쪽 자식으로만 연결
- 루트 = 헤드, 오른쪽 자식 = 넥스트

```cpp
void InsertFront(Data d) {
    Node* newNode = new Node(d);
    newNode->right = root;  // left는 항상 null
    root = newNode;
}

void PrintList(Node* n) {
    while (n != nullptr) {
        cout << n->data;
        n = n->right;
    }
}
```


## Expression Tree (수식 트리)

- 내부 노드: 연산자(`+`, `-`, `*`, `/`)
- 리프 노드: 피연산자(숫자)
- **Inorder** 출력 → infix 수식
- **Postorder** 계산 → 아래에서 위로 올라가며 계산 (postfix evaluation)


## Variable Length Code (가변 길이 코드)

| 구분 | 설명 |
|---|---|
| Fixed Length Code | 모든 문자 동일한 길이 |
| **Variable Length Code** | 자주 쓰이는 문자 → 짧은 코드, 드문 문자 → 긴 코드 |

- 트리로 표현: **왼쪽 자식 = 0**, **오른쪽 자식 = 1**
- 내부 노드(`I`): 빈 노드, 리프 노드: 실제 문자

### Decode
루트부터 시작 → 비트가 0이면 왼쪽, 1이면 오른쪽 → 리프 도달 시 해당 문자 출력

### Encode
Inorder 순회로 인코딩 테이블 생성

## Binary Search Tree (BST)

### 특성
- 왼쪽 서브트리의 모든 노드 **< 현재 노드**
- 오른쪽 서브트리의 모든 노드 **≥ 현재 노드**
- 탐색(Search)에 최적화된 트리

### ✅ 시간복잡도
| 연산 | 평균 | 최악 |
|---|---|---|
| Insert | O(log n) | O(n) |
| Search | O(log n) | O(n) |
| Delete | O(log n) | O(n) |

> 최악: 트리가 한쪽으로만 쭉 뻗은 경우 (Skewed Tree)

### Insert
```cpp
Node* Insert(Node* n, Data d) {
    if (n == nullptr) return new Node(d);
    if (d < n->data)
        n->left = Insert(n->left, d);
    else
        n->right = Insert(n->right, d);
    return n;
}
```

### Search
```cpp
Node* Search(Node* n, Data d) {
    if (n == nullptr) return nullptr;   // 못 찾음
    if (d == n->data) return n;         // 찾음
    if (d < n->data)
        return Search(n->left, d);
    else
        return Search(n->right, d);
}
```

### Delete
- 복잡한 절차 필요: 부모 노드를 먼저 찾아야 함
- 케이스 3가지: 리프 노드 / 자식 1개 / 자식 2개


## BST vs Binary Search vs Heap

| 항목 | Sorted Array (Binary Search) | Heap | BST |
|---|---|---|---|
| Search | O(log n) 보장 | O(n) | O(log n) 평균, O(n) 최악 |
| Insert | O(n) | O(log n) | O(log n) 평균 |
| 구조 | 배열 | 완전 이진 트리 (위↔아래 관계) | 이진 트리 (좌<노드≤우) |

- **Heap**: 위↔아래 관계만 유지 (최대/최솟값 빠른 접근)
- **BST**: 좌우 관계 유지 → 탐색, 삽입 모두 유연


## BST 밸런스 문제

- **Balanced BST**: 왼/오른 서브트리 노드 수가 비슷 → 높이 O(log n)
- **Skewed BST**: 한쪽으로만 뻗음 → 높이 O(n), 탐색 O(n)

### 실제 문제 상황
- 데이터가 항상 랜덤하지 않음
- 예: 학번(연도 순서대로 증가) → 매번 오른쪽에만 삽입 → Skewed Tree 생성
- 해결책: **AVL Tree**, **Red-Black Tree** 등 자가 균형 BST (추후 학습)


## BST 응용 예시

### 1. Dictionary (사전)
```cpp
struct Word {
    string word;
    string meaning;
};
// BST에서 단어 알파벳 순으로 insert/search
```

### 2. Student BST
- Student(id, name, age)를 BST의 노드로 사용
- id 기준으로 insert/search
- Inorder 출력 시 id 오름차순으로 정렬 출력


