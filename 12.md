## Directed Graph (방향 그래프)
- 간선에 방향이 있는 그래프
- Undirected Graph와 달리 inDegree / outDegree 구분
- inDegree: 해당 정점으로 들어오는 간선 수
- outDegree: 해당 정점에서 나가는 간선 수
- BFS, DFS 모두 적용 가능
- Weakly Connected: 방향 간선을 모두 무방향으로 바꿨을 때 연결 그래프가 되면 Weakly Connected

#### ✅ 시간복잡도
InsertVertex: O(1)  
InsertEdge: O(1)  
DFS/BFS: O(V + E)  
TopologicalSort: O(V + E)  

#### ✅ 구현
```cpp
class Graph {
    char label[GRAPH_SIZE];
    bool adjacent[GRAPH_SIZE][GRAPH_SIZE];
    int degree[GRAPH_SIZE];
    bool visited[GRAPH_SIZE];
};

class DirectedGraph : public Graph {
    int inDegree[GRAPH_SIZE];
    int outDegree[GRAPH_SIZE];
    // Optional: PrintGraph
};
```

✅ Weakly Connected 판별
```cpp
class CheckWeaklyConnected : public DirectedGraph {
    void DepthFirstSearch2(int m) {
        visited[m] = true;
        for (int i = 0; i < GRAPH_SIZE; i++)
            if (adjacent[m][i] && !visited[i])
                DepthFirstSearch2(i);
    }
    void CheckConnected() {
        cout << "Depth First Search: ";
        Reset(visited);
        DepthFirstSearch2(0); cout << endl;
        bool isConnected = true;
        for (int i = 0; i < GRAPH_SIZE; i++)
            if (!visited[i]) isConnected = false;
        if (isConnected)
            cout << "Weakly Connected" << endl;
        else
            cout << "Unconnected" << endl;
    }
};
```

---

## Topological Sort (위상 정렬)
- DAG(Directed Acyclic Graph)에서만 수행 가능 (사이클 없는 방향 그래프)
- 모든 간선 u→v에 대해 u가 v보다 먼저 나오는 순서
- inDegree가 0인 정점부터 시작

#### ✅ 시간복잡도
O(V + E)

#### ✅ 알고리즘
```
Queue에 inDegree가 0인 모든 정점 삽입
while Queue is not empty:
    v = Queue.Delete()
    v 출력
    for each neighbor w of v:
        w의 inDegree -= 1
        if w의 inDegree == 0:
            Queue.Insert(w)
```

---

## Application: Path Finding
- 그래프에서 두 정점 사이의 경로를 찾는 문제
- DFS 또는 BFS를 이용해 경로 탐색
- 최단 경로가 아닌 단순 경로 탐색 시 DFS 활용

---

## Weighted Graph (가중 그래프)
- 간선에 가중치(weight)가 있는 그래프
- 가중치는 Cost(비용), Time(시간), Distance(거리) 등을 표현
- **최단 경로**: 경로 위 가중치 합의 최솟값 → Min of Sum(weight on a path)
- **최대 용량(Capacity)**: 경로 위 가중치의 최솟값의 최댓값 → Max of Min(weight on a path)

#### ✅ 인접 행렬 표현 (Adjacency Matrix)
```cpp
class WeightedGraph : public Graph {
    int weight[GRAPH_SIZE][GRAPH_SIZE];
    // 간선 없으면 INF 또는 0으로 초기화
};
```

#### ✅ 연결 리스트 표현 (Adjacency Linked List)
```cpp
struct Node {
    int vertex;
    int weight;
    Node* next;
};

class GraphLinkedList {
    Node* adj[GRAPH_SIZE];
};

class WeightedGraphLinkedList : public GraphLinkedList {
    // weight 포함 edge 삽입
};
```

---

## Minimum Spanning Tree (최소 신장 트리)
- 모든 정점을 연결하는 사이클 없는 트리 중 가중치 합이 최소인 것
- 알고리즘: **Prim's Algorithm**, Kruskal's Algorithm

#### ✅ Prim's Algorithm
1. 임의의 정점에서 시작
2. 현재 MST와 연결된 간선 중 가중치 최소 간선 선택
3. 사이클이 생기지 않으면 추가
4. 모든 정점이 포함될 때까지 반복

---

## Shortest Path (최단 경로)
- 두 정점 사이의 가중치 합이 최소인 경로
- 알고리즘: **Dijkstra's Algorithm** (음수 가중치 없을 때)

#### ✅ 시간복잡도
Dijkstra (인접 행렬): O(V²)  
Dijkstra (우선순위 큐): O((V + E) log V)  

#### ✅ Dijkstra's Algorithm
```
dist[source] = 0, 나머지 = INF
visited = {}

while 미방문 정점 존재:
    u = dist 최소인 미방문 정점
    visited에 u 추가
    for each neighbor v of u:
        if dist[u] + weight(u,v) < dist[v]:
            dist[v] = dist[u] + weight(u,v)
```

#### ✅ 구현 (Usage 예시)
```cpp
#include "WeightedGraph.h"

int main() {
    WeightedGraph g;
    g.InsertVertex(0, 'S');
    g.InsertVertex(1, 'A');
    g.InsertVertex(2, 'B');
    g.InsertVertex(3, 'C');
    g.InsertVertex(4, 'D');
    g.InsertEdge(0, 1, 10);
    g.InsertEdge(0, 3, 5);
    g.InsertEdge(1, 2, 4);
    g.InsertEdge(2, 4, 12);
    g.InsertEdge(3, 1, 3);
    g.InsertEdge(3, 4, 4);

    cout << "Minimum Spanning Tree: " << endl;
    g.MinimumSpanningTree();

    cout << "Shortest Path: " << endl;
    g.ShortestPath();

    return 0;
}
```